% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sharpen.R
\name{sharpen_density}
\alias{sharpen_density}
\title{Data-sharpened density estimation}
\usage{
sharpen_density(
  x,
  m = Inf,
  comparator = c("sinc", "trapezoid"),
  bw = NULL,
  comparator_bw = NULL,
  kernel = "gaussian",
  breaks = c("equal", "modal"),
  min_block = NULL,
  max_shift = 1,
  polish = TRUE,
  control = list(),
  n = 512,
  from = NULL,
  to = NULL
)
}
\arguments{
\item{x}{Numeric vector of observations.}

\item{m}{Number of distinct shift values. `Inf` (default) or 3.}

\item{comparator}{`"sinc"` (default) or `"trapezoid"`.}

\item{bw, comparator_bw, breaks, min_block}{As in [tilt_density()]. Breakpoint
search is not available here, so `breaks` defaults to `"equal"`; supply
`"modal"` or an explicit numeric vector if you want different boundaries.}

\item{kernel}{Kernel of the estimator; see [tilt_kernels()].}

\item{max_shift}{Half-width of the box searched, as a multiple of the data
range (default 1).}

\item{polish}{Refine the search result with [stats::optim()] (default `TRUE`).}

\item{control}{A list of options passed to the genetic algorithm; see
[real_ga()].}

\item{n, from, to}{Grid on which to evaluate the fitted density.}
}
\value{
An object of class `"tiltdens"`; see [tilt_density()]. The `shifts`
  and `sharpened` components hold `q` and `x + q`.
}
\description{
Finds shifts `q` for the estimator
\deqn{\hat f(x \mid h, q) = \frac{1}{nh}\sum_i K\!\left(\frac{x - x_i - q_i}{h}\right)}
by minimising its L2 distance to an infinite-order comparator. This is the
second form of data perturbation in Doosti and Hall (2016): the observations
are moved rather than re-weighted.
}
\details{
The objective is not convex, so the result depends on the random seed. Set
one before calling if you need reproducibility. With `m = Inf` the search is
over `length(x)` variables and is slow; the paper reports run times of a few
minutes per sample of size 100. In the published simulations tilting was
usually at least as accurate and far cheaper, so [tilt_density()] and
[tilt_density_cv()] are the better default.
}
\examples{
set.seed(1)
x <- c(rnorm(40, -1.5), rnorm(40, 1.5))

## Small budget so the example runs quickly.
fit <- sharpen_density(x, m = 3,
                       control = list(max_iterations = 20, population = 20))
plot(fit)

}
\references{
Doosti, H. and Hall, P. (2016). Making a non-parametric density estimator
more attractive, and more accurate, by data perturbation.
*Journal of the Royal Statistical Society B* **78**, 445-462.
}
\seealso{
[tilt_density()], [tilt_density_cv()]
}
